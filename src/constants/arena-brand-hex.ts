/**
 * Hex para APIs que não resolvem CSS variables (Stripe iframe, janela de impressão, etc.).
 * Manter alinhado a `:root` em `src/app/globals.css`.
 */
export const ARENA_BRAND_HEX = {
  navy800: "#002b40",
  navy900: "#001d2d",
  button: "#ff6b00",
  buttonHover: "#e66000",
  accent: "#ff8e3d",
  soft: "#f8fafc",
  appSurface: "#f8f9fa",
} as const
